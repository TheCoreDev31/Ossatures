/*function createWindow() {
    let windowMaterial = new THREE.MeshPhongMaterial({
        color: 0xffffff,
        shininess: 70,
        specular: 0x99ccff,
        refractionRatio: 0.8
    });
    let frontWindow = new THREE.Mesh(new THREE.BoxBufferGeometry(12, 14, 3), windowMaterial)
    frontWindow.position.set(0, 5.2, longueurModule / 2);
    scene.add(frontWindow);
}

export {
    createWindow
}
*/
